"""Repository-level developer tools.

Not packaged into the `jss-style-checker` wheel (see
``tool.hatch.build.targets.wheel.packages`` in ``pyproject.toml``); present
in the sdist so ``git clone`` + ``pip install -e '.[dev]'`` surfaces them.
"""
