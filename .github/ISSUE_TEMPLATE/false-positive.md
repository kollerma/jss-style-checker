---
name: False positive
about: The checker flagged something that is actually fine
labels: false-positive
---

**Rule id** (from the report, e.g. `JSS-MARKUP-002`):

**The flagged line(s)** (paste the LaTeX/BibTeX source):

```tex

```

**Why it is a false positive**:

**How you ran the checker** (CLI / R / Python / VS Code / web app, and
flags if any):

<!-- False-positive reports feed directly into the per-rule precision
     measurement — thank you. If you can, check whether the violation
     reproduces on the web app (https://kollerma.github.io/jss-style-checker/)
     so we know it's not channel-specific. -->
