---
name: Bug report
about: Crash, wrong output, parity mismatch, or install problem
labels: bug
---

**What happened**:

**What you expected**:

**Steps to reproduce** (a minimal `.tex`/`.bib` snippet if the input
matters):

```tex

```

**Channel and version** (e.g. `jss-lint --version`, `jsslint --version`,
R `packageVersion("jsslintr")`, VS Code extension version, or "web app"):

**Platform** (OS; browser if using the web app):
