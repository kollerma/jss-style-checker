# Adversarial review round (2026-07-12) — findings and fix plan

Three adversarial reviews (JSS editor screen, JSS author hands-on, JSS
technical referee) of the 2026-07-11 manuscript. Consolidated verdict:
major revision. This file is the fix ledger; work lands on branch
`020-review-fixes`.

## Findings → dispositions

### Workstream 1 — tool & data (evidence layer)

| # | Finding (source) | Disposition |
|---|---|---|
| F1 | 97.3% precision ignores measured labeler error: 88.5% of live labels are AI-issued; gold set shows 6.9% (Qwen3) / 13.7% (Bonsai) error on TP verdicts → honest ≈90–94%; benchmark result never reported; human-only subset = 94.1% (referee 1) | Generator computes and emits provenance + adjusted-interval macros from a **committed labels export**; manuscript reframes (W2) |
| F2 | Gold-set contamination episode (268 AI labels under `human:*`, incl. AI overriding humans) undisclosed (referee 2) | Disclose as third audit finding in §5.6 (W2); no data fix needed (already decontaminated 2026-07-06) |
| F3 | Corpus arithmetic 232+16+6=254≠255; `jss5342-versions` absent from pinned manifest; "255 real" includes 6 synthetic (referee 5) | Generator: single source of truth = manifest; investigate `jss5342-versions` and either add manifest row(s) or report "254 pinned + 1 documented exception"; separate real/synthetic counts into distinct macros |
| F4 | Raw labels gitignored — headline number unauditable; gold set + routing not shipped (editor 3, referee 8) | Commit `eval/labels-export.csv.gz` (paper_key, rule_id, line, verdict, reviewer_class, verdict_reason), the 1,410-row gold set, and the routing table; generator reads the export |
| F5 | CAP-004 flags lowercase-first keywords — the journal's actual published convention; message/example/behavior disagree (author 3) | Narrow rule in BOTH engines (drop the leading-capital requirement; keep no-Title-Case-across-entries), align suggestion text (suggestion is not part of the label key; message_template must NOT change), tests first; pure narrowing → no new pending labels |
| F6 | Performance table hardware unstated (linuxkit VM) (referee 7) | Generator emits `\StatBenchMachine`; caption states it (W2) |
| F7 | British spellings in generated appendix (editor 5b) | American-ize in the appendix *renderer* only (paraphrase column; do NOT touch catalogue `description` fields — they are message templates and part of the eval label key and Rust parity surface); extend spelling gate to `generated/*.tex` (W2 Makefile) |

After F1–F7: full corpus rescan → record iteration 112 → recall re-run
→ re-pin generator constants → regenerate. CAP-004 narrowing only
removes firings, so 0-pending is preserved.

### Workstream 2 — manuscript

| # | Finding (source) | Disposition |
|---|---|---|
| F8 | Abstract/§6.2 present 97.3% as tool precision with implicit ±0.2 CI (referee 1) | Reframe: label-set estimate; report gold-set agreement, AI-label share, human-only precision, propagated interval; apply the paper's own fallible-labels thesis to its headline |
| F9 | Recall framing: macro 88.4% leads; worst high-volume rules only in appendix (referee 3, editor 8) | Abstract quotes micro only; §6.3 names HOUSE-001 0.533, MARKUP-003 0.596, OPER-004 0.603, REFS-003 0.677 |
| F10 | Published-paper selection bias unacknowledged (referee 4) | New limitations paragraph in §6.1: corpus = already-copy-edited papers; submissions are the harder target; CODE-003 episode as evidence |
| F11 | CODE-003 §6.4: fix author == re-adjudicator, undisclosed (referee 6) | Disclosure sentence; note human spot-check of the six as open item |
| F12 | PyPI naming contradiction §3.2 `jsslint` vs §7 `jss-style-checker`; import module `texlint` unnamed; no R usage in §7; "standard channel" overclaim (author 1/5, editor 4) | Determine canonical naming from publish workflows; one naming table in §7 (ecosystem → install → import/CLI); add R listing (`jsslintr`); honest availability sentence (registries pending at submission gate) |
| F13 | `--flags` render as single-hyphen/en-dash in PDF (author 2, verified visually) | Disable the `--` tt ligature in the preamble; verify glyphs in rendered PDF; note candidate rule in follow-ups |
| F14 | Appendix longtable captions at top; own rule TYPO-004 blind to longtable; checklist claim falsified (editor 5a) | Move captions below (longtable foot); correct checklist; disclose the blindness in §6.4 limitations + follow-ups entry |
| F15 | Demo presented as exhaustive/compliant while tool misses bare `lme4` (MARKUP-002 whitelist), sentence-final "R.", hand-written citations (author 4) | One honest sentence in §2.2 tying the demo's own misses to §6's recall gaps; fix §8 "compliant demonstration manuscript" wording; follow-ups: lme4 whitelist |
| F16 | Silent-on-success, `--fail-on` semantics, dry-run's full output undocumented (author 6) | Add to §7.1/§7.2 |
| F17 | Comparison omits lintr/styler analogy + publisher-side checkers; "only us" framing (editor 7) | One paragraph in §8 |
| F18 | Replication epistemics: consistency ≠ validation (referee 8) | One sentence in §6.5; point to labels export (F4) for spot-checks |
| F19 | AI labelers anonymous (editor 8) | Name models + versions in §5.6 |

### Workstream 2 — resolution (2026-07-12, branch `020-review-fixes`)

All manuscript findings landed; page count 24 (≤ 30), self-lint exit 0,
`make spelling`/`--audit`/`--check`/`replicate.sh` green.

| # | Status | Where |
|---|--------|-------|
| F8 | **done** | Abstract precision triangle (label-set 97.3% / human-only 94.1% / propagated 85.4–91.3%); §6.2 full triangle + formula + conservative-band rationale; §5.6 gold agreement + TP-error + model names; §8 summary bracketed; 97.3% never stands alone |
| F9 | **done** | Abstract recall micro-only; §6.3 names HOUSE-001 0.533, MARKUP-003 0.596, OPER-004 0.603, REFS-003 0.677 (verified against `tab-recall.tex`) |
| F10 | **done** | §6.1 selection-bias paragraph + corpus arithmetic fixed (254 pinned = 232 CRAN + 16 GitHub + 6 synthetic, separated from "real"; auxiliary dir; 255 scanned); intro/abstract "real manuscripts" → "JSS-format manuscripts" |
| F11 | **done** | §6.4 discloses same-workflow re-adjudication of the six CODE-003 rows; human spot-check tracked in follow-ups |
| F12 | **done** | §3.2 names the pure-Python `jss-style-checker` vs Rust wheel `jsslint`; §7 install table (Table 5) + honest availability sentence (registries pending) + R `jsslintr::render` listing |
| F13 | **done** | Preamble `microtype` + `\DisableLigatures[-]{... family=tt*}`; verified in PDF (Table 1, §7); follow-ups entry for a collapsed-flag rule |
| F14 | **done** | Three appendix longtables → captions below via `\endlastfoot`; verified in PDF (heads/feet intact); checklist L153 corrected; §6.4 + follow-ups disclose TYPO-004 longtable blindness |
| F15 | **done** | §2.2 discloses missed bare `\pkg{lme4}` + count 7 confirmed; CAP-004 wording → "title-cased keyword entry"; §7 "compliant" → "corrected demonstration manuscript"; follow-ups lme4/common-package whitelist |
| F16 | **done** | §7.2 documents silent-on-success + exit 0/1/2 + `--fail-on` default `warning` (info advisories do not gate); §7.1 notes `--fix --dry-run` prints diff then remaining-violations table |
| F17 | **done** | §7 comparison paragraph positions `lintr`/`styler` + publisher-side checkers, softens "only us" (no journal ground truth); bib entries `lintr`, `styler` added |
| F18 | **done** | §6.5 sentence: replication = consistency, not label validity; points to `eval/labels-export.csv.gz` + gold-set export |
| F19 | **done** | §5.6 names Bonsai-8B (MLX) and Qwen3-30B-A3B |
| Spelling gate | **done** | `paper/Makefile` `spelling:` now also scans `generated/*.tex` |

### Deferred to the human author (submission gate, unchanged)

Author identity/affiliation; registry publishes (PyPI/crates/npm/
marketplace/CRAN); Zenodo DOI + CITATION.cff; public repo URL;
optional human re-ruling of the six CODE-003 rows; British tool
*messages* (changing `message_template` churns the eval label key —
documented constraint, follow-ups); MARKUP-002 whitelist expansion
(new firings need labeling infrastructure not available on this host).

## Macro contract (W1 defines, W2 consumes)

`\StatLabelsAIShare`, `\StatLabelsHuman`, `\StatLabelsAI`,
`\StatLabelsAuto`, `\StatGoldTPErrQwen`, `\StatGoldTPErrBonsai`,
`\StatGoldAgreementQwen`, `\StatGoldAgreementBonsai`,
`\StatPrecisionHumanOnly`, `\StatPrecisionAdjLow`,
`\StatPrecisionAdjHigh`, `\StatCorpusPinned`, `\StatCorpusSynthetic`,
`\StatBenchMachine` — plus re-pinned existing macros at iteration 112.

## Verification (unchanged gates)

`make -C paper check` (drift, lint, spelling incl. generated/, audit),
`bash paper/replicate.sh` green, PDF glyph check for `--fix`,
longtable captions visually below, page budget ≤ 30.
