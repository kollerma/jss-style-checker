# `placeholder_clean` — smoke vignette, no violations

Placeholder vignette that satisfies every Step-1 smoke rule. Used to
confirm that `eval-jss scan` produces zero violations and exits 0 when
given compliant input.

**Source**: hand-authored placeholder. Replace with a real CRAN vignette
once the 10-paper Phase A corpus is curated (see `examples/README.md`).
