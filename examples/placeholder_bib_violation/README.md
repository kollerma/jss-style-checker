# `placeholder_bib_violation` — smoke vignette, 1 x JSS-BIB-001

Placeholder vignette whose `refs.bib` has exactly one entry with no
`year` field, triggering `JSS-BIB-001`. Exercises the BibTeX-AST rule
code path during Phase A plumbing verification.

**Source**: hand-authored placeholder. Replace with a real CRAN
vignette once the 10-paper Phase A corpus is curated (see
`examples/README.md`).
