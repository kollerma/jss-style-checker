# `placeholder_src_violation` — smoke vignette, 1 x JSS-SRC-001

Placeholder vignette that triggers exactly one `JSS-SRC-001` violation
(a single line exceeds the 80-column default). Used to exercise the
raw-source-scan code path during Phase A plumbing verification.

**Source**: hand-authored placeholder. Replace with a real CRAN vignette
once the 10-paper Phase A corpus is curated (see `examples/README.md`).
