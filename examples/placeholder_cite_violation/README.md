# `placeholder_cite_violation` — smoke vignette, 1 x JSS-CITE-001

Placeholder vignette that triggers exactly one `JSS-CITE-001` violation
(citation wrapped in `\emph` instead of `\cite`). Used to exercise the
AST-inspection code path during Phase A plumbing verification.

**Source**: hand-authored placeholder. Replace with a real CRAN vignette
once the 10-paper Phase A corpus is curated (see `examples/README.md`).
