//! Phase 5 stub. `extendr`/`rextendr` wiring (plus the `R/`, `DESCRIPTION`,
//! `NAMESPACE` package scaffolding) lands once `jsslint-core` has a real
//! engine — deliberately not added yet since `extendr-api` needs libR
//! headers this devcontainer doesn't have.

pub fn placeholder() -> &'static str {
    "jsslint-r: not yet implemented (spec 018, Phase 5 pending)"
}
