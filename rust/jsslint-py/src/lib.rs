//! Phase 5 stub. `pyo3`/`maturin` wiring lands once `jsslint-core` has a
//! real engine — this binding doubles as the in-process parity oracle
//! (call both the Python `texlint` engine and this crate on the same
//! parsed input from one Python test) once it exists.

pub fn placeholder() -> &'static str {
    "jsslint-py: not yet implemented (spec 018, Phase 5 pending)"
}
