<!-- SPECKIT START -->
Current implementation plan: `specs/003-jss-rule-catalogue/plan.md`

For technologies used, project structure, shell commands, and other
important information about the active feature, read that plan and the
sibling artifacts under `specs/003-jss-rule-catalogue/` (`research.md`,
`data-model.md`, `contracts/`, `quickstart.md`).

The previous features' artifacts remain available under
`specs/001-linter-foundation/` and `specs/002-eval-jss-harness/` —
consult them when working on the rule API (spec 001) or the `eval-jss`
precision harness this spec uses as the ≥90% quality gate (spec 002).
<!-- SPECKIT END -->
