<!-- SPECKIT START -->
Current implementation plan: `specs/009-explain-command/plan.md`

For technologies used, project structure, shell commands, and other
important information about the active feature, read that plan and the
sibling artifacts under `specs/009-explain-command/` (`research.md`,
`data-model.md`, `contracts/`, `quickstart.md`).

The previous features' artifacts remain available under
`specs/001-linter-foundation/` through `specs/008-auto-fix/` —
consult them for the rule API (001), the eval-jss harness (002), the
catalogue (003), per-category rule modules (004), `.Rnw` / `.Rmd`
parser extensions (005), the SARIF 2.1.0 renderer (006), the
JSS-guide citation surface (007), and the auto-fix engine (008) on
top of which the explain subcommand (009) extends rule metadata with
prose explanations.
<!-- SPECKIT END -->
