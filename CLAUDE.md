<!-- SPECKIT START -->
Current implementation plan: `specs/004-jss-rule-modules/plan.md`

For technologies used, project structure, shell commands, and other
important information about the active feature, read that plan and the
sibling artifacts under `specs/004-jss-rule-modules/` (`research.md`,
`data-model.md`, `contracts/`, `quickstart.md`).

The previous features' artifacts remain available under
`specs/001-linter-foundation/`, `specs/002-eval-jss-harness/`, and
`specs/003-jss-rule-catalogue/` — consult them for the rule API
(spec 001), the `eval-jss` precision harness this spec uses as the
≥90% quality gate (spec 002), and the rule catalogue whose 58 entries
this spec implements as Python modules (spec 003).
<!-- SPECKIT END -->
