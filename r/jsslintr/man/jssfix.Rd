% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jsslint.R
\name{jssfix}
\alias{jssfix}
\title{Apply automatic fixes for JSS style issues}
\usage{
jssfix(
  files,
  rules = NULL,
  dry_run = FALSE,
  journal = NULL,
  mode = NULL,
  ignore_rules = NULL,
  min_confidence = NULL,
  verbose = NULL
)
}
\arguments{
\item{files}{Character vector of file paths, or a \code{"jsslint"}
object as returned by \code{\link[=jsslint]{jsslint()}} (its files and lint options are
reused).}

\item{rules}{Character vector of rule ids; when given, only fixes
for these rules are applied.}

\item{dry_run}{If \code{TRUE}, show the diff of what would change and
write nothing.}

\item{journal}{Journal identifier (default: \code{"jss"}).}

\item{mode}{\code{"author"} or \code{"reviewer"} (default: \code{"author"}).}

\item{ignore_rules}{Character vector of rule ids to suppress.}

\item{min_confidence}{\code{"low"}, \code{"medium"}, or \code{"high"}.}

\item{verbose}{Enable diagnostic output on stderr.}
}
\value{
Invisibly, an object of class \code{"jsslint_fixes"} with data
frames \code{applied}, \code{skipped}, and \code{rejected}, plus the unified
\code{diff} (dry runs only). Printing it summarises what happened.
}
\description{
Lints the given files and applies the automatic fixes in place
(atomic write; a fix that reintroduces its own violation is rolled
back). Use \code{dry_run = TRUE} to preview the changes as a unified
diff without touching the files.
}
\examples{
\dontrun{
jssfix(c("paper.tex", "refs.bib"), dry_run = TRUE)
jssfix(c("paper.tex", "refs.bib"))
}
}
