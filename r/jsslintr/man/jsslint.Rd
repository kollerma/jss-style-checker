% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jsslint.R
\name{jsslint}
\alias{jsslint}
\title{Check a paper against the JSS style guide}
\usage{
jsslint(
  files = jss_files(),
  journal = NULL,
  mode = NULL,
  ignore_rules = NULL,
  min_confidence = NULL,
  verbose = NULL
)
}
\arguments{
\item{files}{Character vector of paths to \code{.tex}/\code{.ltx} and \code{.bib}
files, e.g. \code{c("paper.tex", "refs.bib")}. Defaults to every such
file in the working directory.}

\item{journal}{Journal identifier (default: \code{"jss"}).}

\item{mode}{\code{"author"} or \code{"reviewer"} (default: \code{"author"}).}

\item{ignore_rules}{Character vector of rule ids to suppress.}

\item{min_confidence}{\code{"low"}, \code{"medium"}, or \code{"high"}.}

\item{verbose}{Enable diagnostic output on stderr.}
}
\value{
An object of class \code{"jsslint"}. Print it for a quick
overview, use \code{\link[=summary]{summary()}} for a by-rule and by-category
breakdown, and \code{\link[=as.data.frame]{as.data.frame()}} for one row per issue. Pass it
to \code{\link[=jssfix]{jssfix()}} to apply the automatic fixes.
}
\description{
Reads the given manuscript files and lints them against the Journal
of Statistical Software style guide. This is the main entry point
for interactive use; \code{\link[=render]{render()}} is the lower-level in-memory
binding it wraps.
}
\examples{
\dontrun{
res <- jsslint(c("paper.tex", "refs.bib"))
res
summary(res)
as.data.frame(res)
jssfix(res)
}
}
